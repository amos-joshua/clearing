import 'package:flutter/material.dart';

class IdleCallWidget extends StatelessWidget {
  const IdleCallWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Ready to make a call'),
    );
  }
} 